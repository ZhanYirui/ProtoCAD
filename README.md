# Dataset

The full dataset is not tracked in Git for this project.

Place the complete dataset under:

```text
data/
```

Expected structure:

```text
data/
  M4/
  MAGE/
```

If you need to share the full dataset, prefer a release asset, Git LFS, or external storage instead of a normal Git commit.
